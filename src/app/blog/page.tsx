import BlogList from "@/components/BlogList";

export const metadata = {
  title: "Blog — OTOBZ",
  description: "OTOBZ가 배우고 구축하는 과정들을 기록합니다.",
};

export default function BlogPage() {
  return <BlogList />;
}
